<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.michong_pc.tiku">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".function_activity.zhangjiexunlian" />
        <activity android:name=".Activity_home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".function_activity.train" />
        <activity android:name=".function_activity.ZuoTiBan" />
        <activity android:name=".function_activity.Zuotiban_tiankongti" />
        <activity android:name=".Result.Result_score" />
        <activity android:name=".function_activity.Test"></activity>
    </application>

</manifest>